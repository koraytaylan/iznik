//! Resident memory and CPU time of a process from `/proc`, the one implementation behind every ceiling.
//!
//! Filled by task `pty-harness` of plan 0001; until then this module holds only its documentation.
