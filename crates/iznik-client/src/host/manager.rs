//! The host manager: one task per host, isolation between hosts, and the resume that keeps a pane's bytes across a drop.
//!
//! Filled by task `connection-manager` of plan 0005; until then this module holds only its documentation.
