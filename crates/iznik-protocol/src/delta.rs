//! The numbered deltas, each the smallest thing that can happen to the host model, encoded as the `Delta` payload.
//!
//! Filled by task `deltas-and-reconciler` of plan 0003; until then this module holds only its documentation.
