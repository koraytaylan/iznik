//! The committed zstd dictionary trained on the fidelity corpus, which primes the compressed link from its first frame.
//!
//! Filled by task `stream-compression` of plan 0003; until then this module holds only its documentation.
