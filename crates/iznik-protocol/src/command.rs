//! The session commands, their outcomes and rejection codes, encoded as the `Command` and `CommandResult` payloads.
//!
//! Filled by task `session-command-codec` of plan 0003; until then this module holds only its documentation.
