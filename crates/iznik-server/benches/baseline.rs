//! The performance baseline: input-to-echo latency, throughput, resident memory and startup of the daemon, measured with `std::time::Instant` and reported as a Markdown table.
//!
//! Filled by task `performance-baseline` of plan 0004; until then the benchmark does nothing, and it exists because the manifest declares it.

/// The benchmark's entry point: a stub until task `performance-baseline` replaces its body.
fn main() {}
