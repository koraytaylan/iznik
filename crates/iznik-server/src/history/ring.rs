//! The ring itself: append, range and copy by absolute sequence, without allocating per chunk in steady state.
//!
//! Filled by task `history-ring` of plan 0002; until then this module holds only its documentation.
