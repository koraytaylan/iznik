//! A size-capped rotating log file behind `tracing-subscriber`, because a full disk is worse than no logs.
//!
//! Filled by task `daemon-lifecycle` of plan 0004; until then this module holds only its documentation.
