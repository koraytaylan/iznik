//! The one place that decides what a subscription starts with, contiguous bytes from the ring or the screen as truth, as a pure function over what the ring holds.
//!
//! Filled by task `resume-and-replay` of plan 0003; until then this module holds only its documentation.
