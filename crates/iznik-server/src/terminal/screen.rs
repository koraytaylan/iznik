//! The screen serializer: the mirror's state as VT bytes exact at a sequence, the primary screen remembered across an alternate-screen switch, and the size cap.
//!
//! Filled by task `screen-serializer` of plan 0002; until then this module holds only its documentation.
