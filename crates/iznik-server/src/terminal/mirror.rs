//! The mirror thread's `LocalSet`, the `libghostty-vt` terminal behind each pane, and the policy that answers a program's queries only while nobody is subscribed.
//!
//! Filled by task `terminal-mirror` of plan 0002; until then this module holds only its documentation.
