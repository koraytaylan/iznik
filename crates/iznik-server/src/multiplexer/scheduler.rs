//! One scheduling round: the focused cursor first, then round-robin, a cursor at zero credit skipped, a lagging background cursor marked stale.
//!
//! Filled by task `multiplexer-assembly` of plan 0003; until then this module holds only its documentation.
