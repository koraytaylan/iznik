//! The session registry: the authoritative host model, the panes behind it, and the numbered deltas every change emits.
//!
//! Filled by task `session-registry` of plan 0003; until then this module holds only its documentation.

pub mod commands;
pub mod registry;
