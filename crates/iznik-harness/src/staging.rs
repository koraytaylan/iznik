//! Staging the three static musl binaries the containers run, keyed by content hash, a no-op when nothing changed.
//!
//! Filled by task `regression-fixture` of plan 0001; until then this module holds only its documentation.
