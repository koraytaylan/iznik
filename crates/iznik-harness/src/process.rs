//! The one place a child process is spawned synchronously: in its own process group, under a deadline that ends the whole group and reports what the child said.
//!
//! Filled by task `gate-runner` of plan 0001; until then this module holds only its documentation.
