//! Running a scenario: stage, start the fixture, copy files, run each step in its container, evaluate expectations, enforce the budget, record the overhead.
//!
//! Filled by task `scenario-driver` of plan 0001; until then this module holds only its documentation.
