name = "unsupported"
claims = ["scenario-driver-unsupported"]
driver = "engine"
budget_seconds = 30
exclusive = false

[setup]
hosts = 1
files = []

[[steps]]
id = "not-yet"
container = "engine"
probe = "anything"
timeout_seconds = 10

[[expect]]
step = "not-yet"
exit = 2
stderr_contains = "plan 0005"
