name = "bytes-over-ssh"
claims = ["channel-carries-a-pane-over-ssh"]
driver = "engine"
budget_seconds = 90
exclusive = false

[setup]
hosts = 1
files = []

[[steps]]
id = "a-line-typed-comes-back"
container = "engine"
timeout_seconds = 60

[steps.channel]
alias = "host0"
server = "/iznik/bin/iznik-server"
actions = [
  { kind = "create_session", name = "work" },
  { kind = "subscribe", pane = 1 },
  # The needle is what the shell computes, not what was typed: a
  # pseudoterminal echoes the keystrokes back at once, so waiting for the words
  # of the command would be satisfied before the command ran.
  { kind = "input", pane = 1, text = "echo over-$((6*7))\n" },
  { kind = "await_bytes", pane = 1, contains = "over-42" },
]

[[expect]]
step = "a-line-typed-comes-back"
exit = 0
stdout_contains = "4 actions"
