//! Which tasks a run verifies: explicit ids, everything, or the current branch's diff under the product-code rule.
//!
//! Filled by task `claims-registry` of plan 0001; until then this module holds only its documentation.
