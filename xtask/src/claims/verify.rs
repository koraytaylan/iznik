//! Building the nextest filterset, running the proofs under the `claims` profile, and reading the `JUnit` report.
//!
//! Filled by task `claims-registry` of plan 0001; until then this module holds only its documentation.
