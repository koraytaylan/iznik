//! No blocking standard-library call under the two asynchronous crates, with `pty::streams` as the one named exception.
//!
//! Filled by task `policy-gates` of plan 0001; until then this module holds only its documentation.
