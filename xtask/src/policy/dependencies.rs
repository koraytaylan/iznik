//! The resolved package set equals the allowlist in both directions, every requirement is an exact pin, `iznik-protocol` has no dependencies, and no crate has a build script.
//!
//! Filled by task `policy-gates` of plan 0001; until then this module holds only its documentation.
